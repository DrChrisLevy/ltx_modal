[project]
name = "ltx"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "modal>=1.3.5",
    "python-dotenv>=1.2.2",
]

[dependency-groups]
dev = [
    "ipython>=9.10.0",
    "pillow>=12.1.1",
    "ruff>=0.15.7",
]
